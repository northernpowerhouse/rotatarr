# Example .env file (not used by default — the docker-compose includes inline env variables).
# If you prefer not to edit the compose file, you can still use environment variables or an env file when running,
# but the recommended and supported deployment provided in this repository uses the variables in `docker-compose.yml`.
# For reference only:
PROWLARR_URL=http://localhost:9696
PROWLARR_API_KEY=your_api_key_here
CHECK_INTERVAL_MIN=30
TAG_TO_TRY=flaresolvarr
TAG_FORCE=false
DRY_RUN=true
LOG_LEVEL=INFO
